# GeoAvil 0.3.0-alpha3

Alpha3 restores the alpha1 Shizuku backend and applies only a narrow crash guard to the analog overlay. It intentionally removes the alpha2 bind-wait/fallback rewrite that could leave Shizuku granted while provider setup failed silently.

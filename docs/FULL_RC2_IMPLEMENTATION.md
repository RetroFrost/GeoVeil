# GeoVeil full RC2 implementation branch

This branch is reserved for the complete RC2 engine: companion-backed shared state, one-crash fuse, central Android 16 delivery hooks, standalone root manager control, and joystick movement.

A component is not considered working until its source builds in CI and the target-device release gate passes.
